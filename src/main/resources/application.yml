spring:
#  datasource:
#    url: jdbc:oracle:thin:@(DESCRIPTION=(ADDRESS=(PROTOCOL=TCP)(HOST=localhost)(PORT=1521))(CONNECT_DATA=(SERVICE_NAME=XEPDB1))(CONNECT_TIMEOUT=120)(RETRY_COUNT=20)(RETRY_DELAY=5))
#    driver-class-name: oracle.jdbc.OracleDriver
#    username: THANHTEST
#    password: thanh123
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.OracleDialect
        format_sql: true
  jackson:
    default-property-inclusion: NON_NULL

github:
  oauth:
    client-id: Ov23li05V5giSd6gdMiE
    client-secret: 914135f2759808d961412c1c938718f026b4e48a
    redirect-uri: http://localhost:8080/api/auth/github/callback
    scope: user:email,repo

jwt:
  secret: starboard_jwt_super_secret_key_minimum_256_bits_long_secure_key_2024
  expiration: 86400000  # 24 hours

server:
  port: ${PORT:8080}

logging:
  level:
    com.starboard: DEBUG
    org.springframework.security: INFO

# Star verification job
# Mặc định: chạy mỗi giờ (0 0 * * * *)
# Ví dụ 5 phút: 0 */5 * * * *
# Ví dụ mỗi ngày lúc 3h sáng: 0 0 3 * * *
star:
  verify:
    cron: "*/5 * * * * *"
